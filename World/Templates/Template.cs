using UnityEngine;

class Template
{
    /*
    //two sides island
    float circle = Mathf.Sqrt(Mathf.Pow(pos.x - 64, 2) + Mathf.Pow(pos.z - 64, 2));
    if (pos.x < circle && pos.z < circle || pos.x > circle && pos.z > circle)
        return 0;

    // hole in center of island
    float hole = Mathf.Pow(pos.x - ellipsoidCenter.x, 2) / Mathf.Pow(ellipsoidSize.x * 2, 2) - Mathf.Pow(pos.y - ellipsoidCenter.y, 2) / Mathf.Pow(ellipsoidSize.y * 6, 2) + Mathf.Pow(pos.z - ellipsoidCenter.z, 2) / Mathf.Pow(ellipsoidSize.z * 2, 2);
    if (hole <= .01f)
        return 0;
    */
}